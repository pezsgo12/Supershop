@model IEnumerable<SuperShop.Web.Models.Shared.IndexCategoryViewModel>

@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="row imagetiles">
        @foreach (var category in Model)
        {
            <partial name="~/Views/Shared/_CategoryPartial.cshtml" model="@category" />
        }
    </div>
</div>
